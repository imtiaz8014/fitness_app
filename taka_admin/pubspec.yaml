name: taka_admin
description: "TAKA Admin - Prediction Market Admin Panel"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.0-268.0.dev

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Firebase
  firebase_core: ^3.12.1
  firebase_auth: ^5.5.2
  cloud_firestore: ^5.6.6
  cloud_functions: ^5.3.4

  # Authentication
  google_sign_in: ^6.3.0

  # State Management
  flutter_bloc: ^9.1.0
  equatable: ^2.0.5
  get_it: ^8.0.3

  # Utils
  intl: ^0.20.2
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
